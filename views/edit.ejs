<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
</head>
<body>
    <h3>Edit Your Post</h1>
    <p>Post id : <%= post.id %></p>
    <p>username : @<%= post.username %></p>
    <form action="/posts/<%= post.id %>?_method=PATCH" method="post">
        <label for="newcontent">New Content : </label>
        <textarea rows="10" cols="50" name="newcontent" placeholder="Edit your Content"><%= post.content %> </textarea>
        <br><br>
        <button>Submit</button>
    </form>
</body>
</html>